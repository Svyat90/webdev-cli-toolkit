
scp -r /dir root@host:/path             # copy dorectory to remote server recursively

tar -czf archive.tar.gz ./              # archive directory using TAR tool
tar -xf archive.tar.gz                  # extract using TAR tool

zip -r archive.zip /dir                 # archive directory using ZIP tool
unzip ~/archive.zip -d ~/dir            # extract using ZIP tool